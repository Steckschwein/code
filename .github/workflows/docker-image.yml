name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: steckschwein/buildenv:v2_19-alpine

    steps:
    - uses: actions/checkout@v3
    - name: Build 
      run: export CC65_HOME=/cc65/ && make
