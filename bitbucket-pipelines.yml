# This is a sample build configuration for Other.
# Check our guides at https://confluence.atlassian.com/x/5Q4SMw for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: steckschwein/buildenv:v2_19

pipelines:
  default:
    - step:
        script:
          - ca65 --version
          - export CC65_HOME=/cc65/
          - make
            #- ( cd asmunit && make clean all )
            #- ( cd steckos && make clean all )
            #- ( cd ehbasic && make clean all )
            #- ( cd edlib && make clean all )
            #- ( cd games/dinosaur && make clean all )
            #- ( cd games/pong && make clean all )
            #- ( cd games/microchess && make clean all )
            #- ( cd steckos/tools/lib/test && make clean all )
            #- ( cd steckos/kernel/test && make clean all )
            #- ( cd steckos/clib/libsrc;make clean all )
            #- ( cd steckos/tools/;make clean all )
